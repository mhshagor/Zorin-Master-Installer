১) Full Permission (read, write, execute)
sudo chmod -R 777 /opt


৩) Owner change করা (root → user)

যদি permission error পান, সাধারণত owner change করলেই ঠিক হয়ে যায়:

sudo chown -R $USER:$USER /path/to/folder

Zorin OS Master Cheat Sheet (Bangla)
১. সিস্টেম তথ্য (System Information)
Command	ব্যাখ্যা
uname -a	Kernel ও সিস্টেম সম্পর্কিত তথ্য।
lsb_release -a	OS ভার্সন ও কোডনেম দেখায়।
hostnamectl	হোস্টনেম ও OS তথ্য।
uptime	সিস্টেম কতক্ষণ চালু আছে ও load।
df -h	ডিস্ক ব্যবহার দেখায়।
free -h	RAM ব্যবহার দেখায়।
top	চলমান প্রসেস, CPU/Memory ব্যবহার।
htop	ইন্টারঅ্যাকটিভ প্রসেস মনিটর (sudo apt install htop)
২. প্যাকেজ ম্যানেজমেন্ট (APT)
Command	ব্যাখ্যা
sudo apt update	প্যাকেজ লিস্ট আপডেট।
sudo apt upgrade -y	সমস্ত ইনস্টল প্যাকেজ আপগ্রেড।
sudo apt full-upgrade -y	অতিরিক্ত নির্ভরতাসহ আপগ্রেড।
sudo apt install <package>	প্যাকেজ ইনস্টল।
sudo apt remove <package>	প্যাকেজ অপসারণ।
sudo apt purge <package>	প্যাকেজ ও config মুছে দেয়।
sudo apt autoremove -y	অপ্রয়োজনীয় ডিপেন্ডেন্সি মুছে দেয়।
sudo apt clean	ডাউনলোড করা প্যাকেজ মুছে দেয়।
sudo apt search <package>	প্যাকেজ খুঁজে বের করে।
৩. সার্ভিস ও সিস্টেম ডেমন (systemd)
Command	ব্যাখ্যা
sudo systemctl status <service>	সার্ভিসের অবস্থা।
sudo systemctl start <service>	সার্ভিস চালু।
sudo systemctl stop <service>	সার্ভিস বন্ধ।
sudo systemctl restart <service>	সার্ভিস রিস্টার্ট।
sudo systemctl enable <service>	বুটে চালু হবে।
sudo systemctl disable <service>	বুটে চালু হবে না।

উদাহরণ:

sudo systemctl restart apache2
sudo systemctl status mysql

৪. ডিস্ক ও ফাইল ম্যানেজমেন্ট
Command	ব্যাখ্যা
ls -l	ফাইলের বিস্তারিত দেখায়।
ls -a	লুকানো ফাইলসহ সব ফাইল দেখায়।
cd /path/to/directory	ডিরেক্টরি পরিবর্তন।
pwd	বর্তমান ডিরেক্টরি।
mkdir <dir>	নতুন ডিরেক্টরি।
rm <file>	ফাইল মুছে দেয়।
rm -r <dir>	ডিরেক্টরি ও সবকিছু মুছে দেয়।
cp <src> <dst>	ফাইল/ডিরেক্টরি কপি।
mv <src> <dst>	ফাইল/ডিরেক্টরি মুভ/রিনেম।
chmod 755 <file>	পারমিশন পরিবর্তন।
chown user:group <file>	মালিকানা পরিবর্তন।
du -sh <dir>	ডিরেক্টরির সাইজ দেখায়।
৫. ইউজার ম্যানেজমেন্ট
Command	ব্যাখ্যা
whoami	কারা লগ ইন করেছে।
id	ইউজার ও গ্রুপ তথ্য।
sudo adduser <username>	নতুন ইউজার।
sudo passwd <username>	পাসওয়ার্ড পরিবর্তন।
sudo deluser <username>	ইউজার মুছে দেয়।
groups <username>	ইউজারের গ্রুপ দেখায়।
৬. নেটওয়ার্ক কমান্ড
Command	ব্যাখ্যা
ip a	নেটওয়ার্ক ইন্টারফেস ও IP।
ping google.com	কানেকশন পরীক্ষা।
curl http://example.com	ওয়েব কন্টেন্ট fetch।
wget http://example.com	ফাইল ডাউনলোড।
nmcli dev status	নেটওয়ার্ক ডিভাইস অবস্থা।
systemctl restart NetworkManager	নেটওয়ার্ক রিস্টার্ট।
৭. ডেভেলপমেন্ট টুলস
Command	ব্যাখ্যা
git --version	Git ভার্সন।
git clone <repo>	রেপোজিটরি ক্লোন।
node -v	Node.js ভার্সন।
npm -v	NPM ভার্সন।
php -v	PHP ভার্সন।
composer --version	Composer ভার্সন।
python3 --version	Python ভার্সন।
pip3 --version	Python pip ভার্সন।
docker --version	Docker ভার্সন।
৮. Snap ও Flatpak
Command	ব্যাখ্যা
snap list	Snap প্যাকেজ লিস্ট।
sudo snap install <package>	Snap প্যাকেজ ইনস্টল।
sudo snap remove <package>	Snap প্যাকেজ রিমুভ।
flatpak list	Flatpak অ্যাপ লিস্ট।
flatpak install flathub <package>	Flatpak ইনস্টল।
flatpak uninstall <package>	Flatpak রিমুভ।
৯. লগ ও ডিবাগিং
Command	ব্যাখ্যা
journalctl -xe	সিস্টেম লগ।
dmesg	Kernel লগ।
cat /var/log/syslog	সিস্টেম লগ ফাইল।
cat /var/log/dpkg.log	প্যাকেজ ইনস্টল/রিমুভ লগ।
১০. রিবুট / শাটডাউন
Command	ব্যাখ্যা
sudo reboot	রিবুট।
sudo shutdown -h now	এখনই শাটডাউন।
sudo shutdown -r +5	৫ মিনিট পরে রিবুট।
১১. অন্যান্য গুরুত্বপূর্ণ কমান্ড
Command	ব্যাখ্যা
history	পূর্বে চালানো কমান্ডের ইতিহাস।
alias	আলিয়াস লিস্ট।
unalias <name>	আলিয়াস মুছে দেয়।
xhost +	GUI অ্যাপ X11 এক্সেস।
sudo apt install -y curl wget git build-essential	ডেভেলপমেন্ট টুল ইনস্টল।

💡 টিপস:

Zorin OS Ubuntu ভিত্তিক, তাই Ubuntu/Debian কমান্ড সব কাজ করবে।

sudo সাবধানতার সঙ্গে ব্যবহার করুন।

সবসময় নতুন প্যাকেজ ইনস্টল করার আগে আপডেট করুন:

sudo apt update && sudo apt upgrade -y
