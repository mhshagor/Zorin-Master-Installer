# 🐧 Zorin OS Master Command Guide (সম্পূর্ণ কমান্ড গাইড)

**Zorin OS এর জন্য সকল গুরুত্বপূর্ণ কমান্ড - বাংলা ব্যাখ্যা সহ**

---

## 📋 সূচিপত্র (Table of Contents)

1. [সিস্টেম তথ্য](#১-সিস্টেম-তথ্য-system-information)
2. [প্যাকেজ ম্যানেজমেন্ট](#২-প্যাকেজ-ম্যানেজমেন্ট-apt)
3. [সার্ভিস ম্যানেজমেন্ট](#৩-সার্ভিস-ও-সিস্টেম-ডেমন-systemd)
4. [ফাইল ও ডিরেক্টরি](#৪-ডিস্ক-ও-ফাইল-ম্যানেজমেন্ট)
5. [ইউজার ম্যানেজমেন্ট](#৫-ইউজার-ম্যানেজমেন্ট)
6. [নেটওয়ার্ক কমান্ড](#৬-নেটওয়ার্ক-কমান্ড)
7. [ডেভেলপমেন্ট টুলস](#৭-ডেভেলপমেন্ট-টুলস)
8. [XAMPP কমান্ড](#৮-xampp-কমান্ড)
9. [Laravel/PHP কমান্ড](#৯-laravelphp-কমান্ড)
10. [Node.js/npm কমান্ড](#১০-nodejsnpm-কমান্ড)
11. [Git কমান্ড](#১১-git-কমান্ড)
12. [Docker কমান্ড](#১২-docker-কমান্ড)
13. [Snap ও Flatpak](#১৩-snap-ও-flatpak)
14. [GNOME/Desktop](#১৪-gnomedesktop-কমান্ড)
15. [Chrome/Brave ব্যাকআপ](#১৫-chromebrave-ব্যাকআপ-পাথ)
16. [লগ ও ডিবাগিং](#১৬-লগ-ও-ডিবাগিং)
17. [সিস্টেম পারফরম্যান্স](#১৭-সিস্টেম-পারফরম্যান্স)
18. [রিবুট/শাটডাউন](#১৮-রিবুট--শাটডাউন)

---

## ১. সিস্টেম তথ্য (System Information)

| কমান্ড           | ব্যাখ্যা                                 |
| ---------------- | ---------------------------------------- |
| `uname -a`       | Kernel ও সিস্টেম সম্পর্কিত সম্পূর্ণ তথ্য |
| `lsb_release -a` | OS ভার্সন ও কোডনেম দেখায়                |
| `hostnamectl`    | হোস্টনেম ও OS বিস্তারিত তথ্য             |
| `uptime`         | সিস্টেম কতক্ষণ চালু আছে ও load average   |
| `df -h`          | ডিস্ক ব্যবহার (human-readable)           |
| `df -i`          | inode ব্যবহার দেখায়                     |
| `free -h`        | RAM ব্যবহার (human-readable)             |
| `lscpu`          | CPU তথ্য বিস্তারিত                       |
| `lsblk`          | ব্লক ডিভাইস লিস্ট                        |
| `lsusb`          | USB ডিভাইস লিস্ট                         |
| `lspci`          | PCI ডিভাইস লিস্ট                         |
| `dmidecode`      | হার্ডওয়্যার তথ্য (sudo প্রয়োজন)        |

**উদাহরণ:**

```bash
# সিস্টেম তথ্য দেখুন
uname -a
lsb_release -a

# ডিস্ক স্পেস চেক করুন
df -h

# RAM ব্যবহার দেখুন
free -h
```

---

## ২. প্যাকেজ ম্যানেজমেন্ট (APT)

| কমান্ড                       | ব্যাখ্যা                            |
| ---------------------------- | ----------------------------------- |
| `sudo apt update`            | প্যাকেজ লিস্ট আপডেট করে             |
| `sudo apt upgrade -y`        | সমস্ত ইনস্টল প্যাকেজ আপগ্রেড        |
| `sudo apt full-upgrade -y`   | অতিরিক্ত নির্ভরতাসহ আপগ্রেড         |
| `sudo apt dist-upgrade`      | ডিস্ট্রিবিউশন আপগ্রেড               |
| `sudo apt install <package>` | নতুন প্যাকেজ ইনস্টল                 |
| `sudo apt install -f`        | ভাঙা ডিপেন্ডেন্সি ঠিক করে           |
| `sudo apt remove <package>`  | প্যাকেজ অপসারণ (config রাখে)        |
| `sudo apt purge <package>`   | প্যাকেজ ও config সম্পূর্ণ মুছে দেয় |
| `sudo apt autoremove -y`     | অপ্রয়োজনীয় ডিপেন্ডেন্সি মুছে দেয় |
| `sudo apt autoclean`         | পুরাতন প্যাকেজ ক্যাশ মুছে দেয়      |
| `sudo apt clean`             | সম্পূর্ণ প্যাকেজ ক্যাশ মুছে দেয়    |
| `apt search <package>`       | প্যাকেজ খুঁজে বের করে               |
| `apt show <package>`         | প্যাকেজের বিস্তারিত তথ্য            |
| `apt list --installed`       | ইনস্টল করা সব প্যাকেজ               |
| `dpkg -l`                    | ইনস্টল করা প্যাকেজ লিস্ট            |
| `dpkg -i <file.deb>`         | .deb ফাইল ইনস্টল                    |

**উদাহরণ:**

```bash
# সিস্টেম আপডেট ও আপগ্রেড
sudo apt update && sudo apt upgrade -y

# নতুন প্যাকেজ ইনস্টল
sudo apt install curl wget git -y

# অপ্রয়োজনীয় প্যাকেজ মুছুন
sudo apt autoremove -y && sudo apt clean
```

---

## ৩. সার্ভিস ও সিস্টেম ডেমন (systemd)

| কমান্ড                                | ব্যাখ্যা                      |
| ------------------------------------- | ----------------------------- |
| `sudo systemctl status <service>`     | সার্ভিসের অবস্থা দেখায়       |
| `sudo systemctl start <service>`      | সার্ভিস চালু করে              |
| `sudo systemctl stop <service>`       | সার্ভিস বন্ধ করে              |
| `sudo systemctl restart <service>`    | সার্ভিস রিস্টার্ট করে         |
| `sudo systemctl reload <service>`     | কনফিগ রিলোড (রিস্টার্ট ছাড়া) |
| `sudo systemctl enable <service>`     | বুটে স্বয়ংক্রিয় চালু হবে    |
| `sudo systemctl disable <service>`    | বুটে চালু হবে না              |
| `systemctl list-units --type=service` | সব সার্ভিস লিস্ট              |
| `systemctl is-active <service>`       | সার্ভিস চালু আছে কিনা         |
| `systemctl is-enabled <service>`      | সার্ভিস enable আছে কিনা       |

**উদাহরণ:**

```bash
# Apache সার্ভিস চেক করুন
sudo systemctl status apache2

# MySQL চালু করুন
sudo systemctl start mysql

# NetworkManager রিস্টার্ট
sudo systemctl restart NetworkManager
```

---

## ৪. ডিস্ক ও ফাইল ম্যানেজমেন্ট

| কমান্ড                          | ব্যাখ্যা                             |
| ------------------------------- | ------------------------------------ |
| `ls -l`                         | ফাইলের বিস্তারিত লিস্ট               |
| `ls -la`                        | লুকানো ফাইলসহ সব ফাইল                |
| `ls -lh`                        | ফাইল সাইজ human-readable             |
| `cd /path/to/directory`         | ডিরেক্টরি পরিবর্তন                   |
| `cd ~`                          | হোম ডিরেক্টরিতে যান                  |
| `cd ..`                         | এক ধাপ উপরে যান                      |
| `pwd`                           | বর্তমান ডিরেক্টরি দেখায়             |
| `mkdir <dir>`                   | নতুন ডিরেক্টরি তৈরি                  |
| `mkdir -p /path/to/nested/dir`  | নেস্টেড ডিরেক্টরি তৈরি               |
| `touch <file>`                  | নতুন ফাইল তৈরি                       |
| `rm <file>`                     | ফাইল মুছে দেয়                       |
| `rm -r <dir>`                   | ডিরেক্টরি ও সবকিছু মুছে দেয়         |
| `rm -rf <dir>`                  | জোর করে মুছে দেয় (সাবধান!)          |
| `cp <src> <dst>`                | ফাইল কপি করে                         |
| `cp -r <src> <dst>`             | ডিরেক্টরি কপি করে                    |
| `mv <src> <dst>`                | ফাইল/ডিরেক্টরি মুভ/রিনেম             |
| `chmod 755 <file>`              | ফাইল পারমিশন পরিবর্তন                |
| `chmod +x <file>`               | এক্সিকিউটেবল করে                     |
| `chown user:group <file>`       | মালিকানা পরিবর্তন                    |
| `chown -R user:group <dir>`     | রিকার্সিভ মালিকানা পরিবর্তন          |
| `du -sh <dir>`                  | ডিরেক্টরির মোট সাইজ                  |
| `du -h --max-depth=1`           | সাব-ডিরেক্টরি সাইজ                   |
| `find / -name <filename>`       | ফাইল খুঁজে বের করে                   |
| `locate <filename>`             | দ্রুত ফাইল খুঁজে (updatedb প্রয়োজন) |
| `tar -czf archive.tar.gz <dir>` | ডিরেক্টরি কম্প্রেস করে               |
| `tar -xzf archive.tar.gz`       | আর্কাইভ এক্সট্র্যাক্ট করে            |

**উদাহরণ:**

```bash
# ফাইল পারমিশন সেট করুন
chmod +x script.sh

# ডিরেক্টরি সাইজ দেখুন
du -sh /var/log

# ফাইল খুঁজুন
find /home -name "*.txt"
```

---

## ৫. ইউজার ম্যানেজমেন্ট

| কমান্ড                             | ব্যাখ্যা                |
| ---------------------------------- | ----------------------- |
| `whoami`                           | বর্তমান ইউজার নাম       |
| `id`                               | ইউজার ও গ্রুপ তথ্য      |
| `sudo adduser <username>`          | নতুন ইউজার তৈরি         |
| `sudo passwd <username>`           | পাসওয়ার্ড পরিবর্তন     |
| `sudo deluser <username>`          | ইউজার মুছে দেয়         |
| `sudo usermod -aG sudo <username>` | ইউজারকে sudo গ্রুপে যোগ |
| `groups <username>`                | ইউজারের গ্রুপ দেখায়    |
| `su - <username>`                  | অন্য ইউজারে সুইচ        |
| `sudo -i`                          | root ইউজারে সুইচ        |

---

## ৬. নেটওয়ার্ক কমান্ড

| কমান্ড                                  | ব্যাখ্যা                         |
| --------------------------------------- | -------------------------------- |
| `ip a`                                  | নেটওয়ার্ক ইন্টারফেস ও IP দেখায় |
| `ip addr show`                          | IP অ্যাড্রেস বিস্তারিত           |
| `ifconfig`                              | নেটওয়ার্ক কনফিগ (পুরাতন)        |
| `ping google.com`                       | কানেকশন পরীক্ষা                  |
| `ping -c 4 google.com`                  | ৪ বার ping করে                   |
| `curl http://example.com`               | ওয়েব কন্টেন্ট fetch             |
| `curl -I http://example.com`            | শুধু হেডার দেখায়                |
| `wget http://example.com/file`          | ফাইল ডাউনলোড                     |
| `wget -c http://example.com/file`       | ডাউনলোড resume                   |
| `nmcli dev status`                      | নেটওয়ার্ক ডিভাইস অবস্থা         |
| `nmcli connection show`                 | নেটওয়ার্ক কানেকশন লিস্ট         |
| `sudo systemctl restart NetworkManager` | নেটওয়ার্ক রিস্টার্ট             |
| `netstat -tuln`                         | পোর্ট লিস্টেনিং চেক              |
| `ss -tuln`                              | সকেট স্ট্যাটিস্টিক্স             |
| `nslookup google.com`                   | DNS লুকআপ                        |
| `dig google.com`                        | DNS বিস্তারিত                    |
| `traceroute google.com`                 | নেটওয়ার্ক রুট ট্রেস             |

**উদাহরণ:**

```bash
# IP অ্যাড্রেস দেখুন
ip a

# ইন্টারনেট কানেকশন চেক
ping -c 4 google.com

# ফাইল ডাউনলোড
wget https://example.com/file.zip
```

---

## ৭. ডেভেলপমেন্ট টুলস

| কমান্ড               | ব্যাখ্যা        |
| -------------------- | --------------- |
| `git --version`      | Git ভার্সন চেক  |
| `node -v`            | Node.js ভার্সন  |
| `npm -v`             | npm ভার্সন      |
| `php -v`             | PHP ভার্সন      |
| `composer --version` | Composer ভার্সন |
| `python3 --version`  | Python ভার্সন   |
| `pip3 --version`     | pip ভার্সন      |
| `docker --version`   | Docker ভার্সন   |
| `code --version`     | VS Code ভার্সন  |
| `mysql --version`    | MySQL ভার্সন    |

---

## ৮. XAMPP কমান্ড

| কমান্ড                              | ব্যাখ্যা         |
| ----------------------------------- | ---------------- |
| `sudo /opt/lampp/lampp start`       | XAMPP চালু       |
| `sudo /opt/lampp/lampp stop`        | XAMPP বন্ধ       |
| `sudo /opt/lampp/lampp restart`     | XAMPP রিস্টার্ট  |
| `sudo /opt/lampp/lampp status`      | XAMPP স্ট্যাটাস  |
| `sudo /opt/lampp/lampp startapache` | শুধু Apache চালু |
| `sudo /opt/lampp/lampp startmysql`  | শুধু MySQL চালু  |
| `/opt/lampp/bin/php -v`             | XAMPP PHP ভার্সন |
| `/opt/lampp/bin/mysql -u root -p`   | MySQL লগইন       |

**Custom Aliases (যদি ইনস্টল করা থাকে):**

```bash
xstart      # XAMPP চালু
xstop       # XAMPP বন্ধ
xrestart    # XAMPP রিস্টার্ট
```

---

## ৯. Laravel/PHP কমান্ড

| কমান্ড                               | ব্যাখ্যা                 |
| ------------------------------------ | ------------------------ |
| `php artisan serve`                  | ডেভেলপমেন্ট সার্ভার চালু |
| `php artisan migrate`                | ডাটাবেস মাইগ্রেশন        |
| `php artisan migrate:fresh`          | ডাটাবেস রিসেট ও মাইগ্রেট |
| `php artisan migrate:fresh --seed`   | রিসেট, মাইগ্রেট ও সিড    |
| `php artisan db:seed`                | ডাটাবেস সিড              |
| `php artisan make:model <Name>`      | মডেল তৈরি                |
| `php artisan make:controller <Name>` | কন্ট্রোলার তৈরি          |
| `php artisan make:migration <name>`  | মাইগ্রেশন তৈরি           |
| `php artisan route:list`             | সব রুট দেখায়            |
| `php artisan cache:clear`            | ক্যাশ ক্লিয়ার           |
| `php artisan config:clear`           | কনফিগ ক্যাশ ক্লিয়ার     |
| `php artisan view:clear`             | ভিউ ক্যাশ ক্লিয়ার       |
| `composer install`                   | ডিপেন্ডেন্সি ইনস্টল      |
| `composer update`                    | ডিপেন্ডেন্সি আপডেট       |
| `composer dump-autoload`             | অটোলোড রিজেনারেট         |

**Custom Aliases:**

```bash
serve       # php artisan serve
dbm         # php artisan migrate
dbfs        # php artisan migrate:fresh --seed
pa          # php artisan
```

---

## ১০. Node.js/npm কমান্ড

| কমান্ড                     | ব্যাখ্যা                 |
| -------------------------- | ------------------------ |
| `npm init`                 | নতুন প্রজেক্ট শুরু       |
| `npm install`              | package.json থেকে ইনস্টল |
| `npm install <package>`    | প্যাকেজ ইনস্টল           |
| `npm install -g <package>` | গ্লোবাল ইনস্টল           |
| `npm uninstall <package>`  | প্যাকেজ রিমুভ            |
| `npm update`               | প্যাকেজ আপডেট            |
| `npm run dev`              | ডেভেলপমেন্ট স্ক্রিপ্ট    |
| `npm run build`            | প্রোডাকশন বিল্ড          |
| `npm run watch`            | ফাইল ওয়াচ মোড           |
| `npm list`                 | ইনস্টল করা প্যাকেজ       |
| `npm list -g`              | গ্লোবাল প্যাকেজ          |
| `npx <command>`            | npm প্যাকেজ রান          |

**Custom Alias:**

```bash
npmrun      # npm run dev
```

---

## ১১. Git কমান্ড

| কমান্ড                     | ব্যাখ্যা               |
| -------------------------- | ---------------------- |
| `git init`                 | নতুন রেপো তৈরি         |
| `git clone <url>`          | রেপো ক্লোন             |
| `git status`               | পরিবর্তন দেখায়        |
| `git add .`                | সব ফাইল স্টেজ করে      |
| `git add <file>`           | নির্দিষ্ট ফাইল স্টেজ   |
| `git commit -m "message"`  | কমিট করে               |
| `git push`                 | রিমোটে পুশ             |
| `git push origin <branch>` | নির্দিষ্ট ব্রাঞ্চে পুশ |
| `git pull`                 | রিমোট থেকে পুল         |
| `git branch`               | ব্রাঞ্চ লিস্ট          |
| `git branch <name>`        | নতুন ব্রাঞ্চ           |
| `git checkout <branch>`    | ব্রাঞ্চ সুইচ           |
| `git checkout -b <branch>` | নতুন ব্রাঞ্চ ও সুইচ    |
| `git merge <branch>`       | ব্রাঞ্চ মার্জ          |
| `git log`                  | কমিট হিস্টোরি          |
| `git log --oneline`        | সংক্ষিপ্ত লগ           |
| `git diff`                 | পরিবর্তন দেখায়        |
| `git reset --hard`         | সব পরিবর্তন বাতিল      |
| `git remote -v`            | রিমোট রেপো দেখায়      |

**Custom Alias:**

```bash
gitpush     # git add . && git commit -m "update" && git push
```

---

## ১২. Docker কমান্ড

| কমান্ড                             | ব্যাখ্যা          |
| ---------------------------------- | ----------------- |
| `docker --version`                 | Docker ভার্সন     |
| `docker ps`                        | চলমান কন্টেইনার   |
| `docker ps -a`                     | সব কন্টেইনার      |
| `docker images`                    | ইমেজ লিস্ট        |
| `docker pull <image>`              | ইমেজ ডাউনলোড      |
| `docker run <image>`               | কন্টেইনার রান     |
| `docker stop <container>`          | কন্টেইনার বন্ধ    |
| `docker rm <container>`            | কন্টেইনার মুছে    |
| `docker rmi <image>`               | ইমেজ মুছে         |
| `docker-compose up`                | কম্পোজ চালু       |
| `docker-compose down`              | কম্পোজ বন্ধ       |
| `docker logs <container>`          | কন্টেইনার লগ      |
| `docker exec -it <container> bash` | কন্টেইনারে প্রবেশ |

---

## ১৩. Snap ও Flatpak

### Snap কমান্ড

| কমান্ড                        | ব্যাখ্যা              |
| ----------------------------- | --------------------- |
| `snap list`                   | ইনস্টল করা Snap লিস্ট |
| `sudo snap install <package>` | Snap ইনস্টল           |
| `sudo snap remove <package>`  | Snap রিমুভ            |
| `snap find <package>`         | Snap খুঁজুন           |
| `snap info <package>`         | Snap তথ্য             |
| `sudo snap refresh`           | সব Snap আপডেট         |

### Flatpak কমান্ড

| কমান্ড                              | ব্যাখ্যা                 |
| ----------------------------------- | ------------------------ |
| `flatpak list`                      | ইনস্টল করা Flatpak লিস্ট |
| `flatpak install flathub <package>` | Flatpak ইনস্টল           |
| `flatpak uninstall <package>`       | Flatpak রিমুভ            |
| `flatpak search <package>`          | Flatpak খুঁজুন           |
| `flatpak update`                    | সব Flatpak আপডেট         |
| `flatpak run <package>`             | Flatpak রান              |

---

## ১৪. GNOME/Desktop কমান্ড

| কমান্ড                            | ব্যাখ্যা                 |
| --------------------------------- | ------------------------ |
| `gnome-shell --version`           | GNOME ভার্সন             |
| `gnome-extensions list`           | এক্সটেনশন লিস্ট          |
| `gnome-extensions enable <uuid>`  | এক্সটেনশন চালু           |
| `gnome-extensions disable <uuid>` | এক্সটেনশন বন্ধ           |
| `gsettings list-schemas`          | সেটিংস স্কিমা লিস্ট      |
| `dconf dump /`                    | সব সেটিংস এক্সপোর্ট      |
| `xdg-open <file>`                 | ডিফল্ট অ্যাপ দিয়ে খুলুন |
| `xdg-open .`                      | ফাইল ম্যানেজার খুলুন     |

---

## ১৫. Chrome/Brave ব্যাকআপ পাথ

### Chrome পাথ

```bash
# Normal Installation
~/.config/google-chrome/

# Snap Installation
~/snap/chromium/common/chromium/

# Flatpak Installation
~/.var/app/com.google.Chrome/config/google-chrome/

# Extensions Path
~/.config/google-chrome/Default/Extensions/

# Bookmarks
~/.config/google-chrome/Default/Bookmarks
```

### Brave পাথ

```bash
# Normal Installation
~/.config/BraveSoftware/Brave-Browser/

# Snap Installation
~/snap/brave/current/.config/BraveSoftware/Brave-Browser/

# Flatpak Installation
~/.var/app/com.brave.Browser/config/BraveSoftware/Brave-Browser/

# Extensions Path
~/.config/BraveSoftware/Brave-Browser/Default/Extensions/
```

---

## ১৬. লগ ও ডিবাগিং

| কমান্ড                        | ব্যাখ্যা                 |
| ----------------------------- | ------------------------ |
| `journalctl -xe`              | সিস্টেম লগ (শেষ এন্ট্রি) |
| `journalctl -f`               | লাইভ লগ                  |
| `journalctl -u <service>`     | নির্দিষ্ট সার্ভিস লগ     |
| `dmesg`                       | Kernel লগ                |
| `dmesg -w`                    | Kernel লাইভ লগ           |
| `cat /var/log/syslog`         | সিস্টেম লগ ফাইল          |
| `cat /var/log/dpkg.log`       | প্যাকেজ লগ               |
| `cat /var/log/auth.log`       | অথেন্টিকেশন লগ           |
| `tail -f /var/log/syslog`     | লাইভ সিস্টেম লগ          |
| `tail -n 100 /var/log/syslog` | শেষ ১০০ লাইন             |

---

## ১৭. সিস্টেম পারফরম্যান্স

| কমান্ড                 | ব্যাখ্যা                     |
| ---------------------- | ---------------------------- | ----------------------- |
| `top`                  | প্রসেস মনিটর                 |
| `htop`                 | ইন্টারঅ্যাকটিভ প্রসেস মনিটর  |
| `ps aux`               | সব প্রসেস লিস্ট              |
| `ps aux                | grep <name>`                 | নির্দিষ্ট প্রসেস খুঁজুন |
| `kill <PID>`           | প্রসেস বন্ধ করে              |
| `kill -9 <PID>`        | জোর করে প্রসেস বন্ধ          |
| `killall <name>`       | নাম দিয়ে প্রসেস বন্ধ        |
| `pkill <name>`         | প্যাটার্ন দিয়ে প্রসেস বন্ধ  |
| `nice -n 10 <command>` | কম প্রায়োরিটিতে রান         |
| `renice -n 5 -p <PID>` | প্রায়োরিটি পরিবর্তন         |
| `iostat`               | I/O স্ট্যাটিস্টিক্স          |
| `vmstat`               | ভার্চুয়াল মেমরি স্ট্যাট     |
| `sar`                  | সিস্টেম অ্যাক্টিভিটি রিপোর্ট |

---

## ১৮. রিবুট / শাটডাউন

| কমান্ড                 | ব্যাখ্যা          |
| ---------------------- | ----------------- |
| `sudo reboot`          | রিবুট করে         |
| `sudo reboot now`      | এখনই রিবুট        |
| `sudo shutdown -h now` | এখনই শাটডাউন      |
| `sudo shutdown -r now` | এখনই রিবুট        |
| `sudo shutdown -r +5`  | ৫ মিনিট পরে রিবুট |
| `sudo shutdown -c`     | শাটডাউন বাতিল     |
| `sudo poweroff`        | পাওয়ার অফ        |
| `sudo halt`            | সিস্টেম হল্ট      |

---

## 🎯 বিশেষ টিপস (Special Tips)

### 1. সিস্টেম আপডেট (সম্পূর্ণ)

```bash
sudo apt update && sudo apt upgrade -y && sudo apt autoremove -y && sudo apt clean
```

### 2. ডিস্ক স্পেস চেক (বিস্তারিত)

```bash
df -h
du -sh /var/log/*
du -sh /home/*
```

### 3. মেমরি ক্লিয়ার

```bash
sudo sync && echo 3 | sudo tee /proc/sys/vm/drop_caches
```

### 4. নেটওয়ার্ক রিসেট

```bash
sudo systemctl restart NetworkManager
sudo systemctl restart systemd-resolved
```

### 5. ব্রাউজার ক্যাশ ক্লিয়ার (ম্যানুয়াল)

```bash
# Chrome
rm -rf ~/.cache/google-chrome/

# Brave
rm -rf ~/.cache/BraveSoftware/
```

### 6. সিস্টেম তথ্য এক নজরে

```bash
echo "=== System Info ===" && \
uname -a && \
echo "=== Disk Usage ===" && \
df -h && \
echo "=== Memory Usage ===" && \
free -h && \
echo "=== Uptime ===" && \
uptime
```

---

## 🔐 নিরাপত্তা টিপস (Security Tips)

1. **sudo সাবধানে ব্যবহার করুন** - শুধু বিশ্বস্ত কমান্ড
2. **rm -rf সাবধানে** - ডেটা হারানোর ঝুঁকি
3. **chmod 777 এড়িয়ে চলুন** - নিরাপত্তা ঝুঁকি
4. **নিয়মিত ব্যাকআপ নিন** - backup.sh ব্যবহার করুন
5. **পাসওয়ার্ড শক্তিশালী রাখুন**

---

## 📚 অতিরিক্ত রিসোর্স (Additional Resources)

- **Zorin OS Forum:** https://forum.zorin.com/
- **Ubuntu Documentation:** https://help.ubuntu.com/
- **Linux Command Line:** https://linuxcommand.org/
- **Bash Scripting:** https://www.shellscript.sh/

---

## 💡 দ্রুত রেফারেন্স (Quick Reference)

### ডেভেলপমেন্ট সেটআপ

```bash
# সম্পূর্ণ ডেভ টুলস ইনস্টল
sudo apt install -y curl wget git build-essential

# Node.js ইনস্টল
curl -fsSL https://deb.nodesource.com/setup_lts.x | sudo -E bash -
sudo apt install -y nodejs

# XAMPP চালু
sudo /opt/lampp/lampp start
```

### Laravel প্রজেক্ট সেটআপ

```bash
composer create-project laravel/laravel myproject
cd myproject
php artisan serve
```

### Git সেটআপ

```bash
git config --global user.name "Your Name"
git config --global user.email "your@email.com"
git init
git add .
git commit -m "Initial commit"
```

---

**📝 নোট:** এই কমান্ডগুলো Zorin OS 16/17 এবং Ubuntu 20.04/22.04 ভিত্তিক সিস্টেমে কাজ করবে।

**Made with ❤️ for Zorin OS Developers**
